public class Main
{
	public static void main(String[] args) {
		int a[]={33,1,42,10,32,11};
		int max1=0,max2=0;
		int size=a.length;
		for(int i=0;i<a.length;i++)
		{
		    if(max1<a[i])
		    {
		        max2=max1;
		        max1=a[i];
		    }
		    if(max2<a[i] && a[i]<max1)
		    {
		        max2=a[i];
		    }

	    System.out.print(max2);
	}
}
